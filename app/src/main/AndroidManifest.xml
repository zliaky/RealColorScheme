<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.realcolorscheme.colorscheme">

    <uses-sdk android:minSdkVersion="21"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ARActivity"></activity>
        <activity android:name=".PaletteActivity"></activity>
        <activity android:name=".PrismaActivity"></activity>
        <activity android:name=".SchemeActivity"></activity>
        <activity android:name=".RecommendActivity"></activity>
    </application>

</manifest>